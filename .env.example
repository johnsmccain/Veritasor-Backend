# Server
PORT=3000

# CORS (optional). Comma-separated origins; in production, unset = no origins allowed. In dev, unset = *.
# ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com

# Shopify OAuth (for POST /api/integrations/shopify/connect and GET /api/integrations/shopify/callback)
# SHOPIFY_CLIENT_ID=
# SHOPIFY_CLIENT_SECRET=
# SHOPIFY_REDIRECT_URI=https://your-api.example.com/api/integrations/shopify/callback
# SHOPIFY_SCOPES=read_orders
# SHOPIFY_SUCCESS_REDIRECT=https://your-app.example.com/integrations/shopify/done

# Database (required for migrations and DB features)
# Use your own local PostgreSQL credentials and database name, e.g.:
# DATABASE_URL=postgresql://YOUR_POSTGRES_USER:YOUR_POSTGRES_PASSWORD@localhost:5432/YOUR_DATABASE_NAME
